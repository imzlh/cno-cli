# Deno Test Coverage Map

Source tree: `/media/sdb1/docs/Documents/tests`

This map tracks how upstream Deno tests are being mined for local `cno` coverage.
It is a working index, not a completion claim.

## Current Local Coverage

| Upstream area | Local coverage | Status |
| --- | --- | --- |
| `unit` Web/Deno APIs | `tests/webapi`, `tests/deno` | Broad coverage added across events, streams, fetch objects, URL, storage, workers, wasm, crypto, fs, command, serve, signals, permissions, cron, kv. |
| `unit_node/_fs` and `fs_test.ts` | `tests/node/fs-*` | Broad callback/sync/promises/FileHandle/watch/stream coverage, including Buffer paths, fd stats, bigint stats, truncate/futimes, abort handling. |
| `unit_node/crypto` and `internal/_random*` | `tests/node/crypto-edge.test.ts`, `tests/webapi/webcrypto-subtle.test.ts` | Broad hash/HMAC/cipher/GCM/sign/KDF/random/key coverage. Added finalized hash/HMAC state and EC explicit parameter rejection. |
| `unit_node/net`, `dgram`, `dns`, `http`, `https`, `tls`, `http2` | `tests/node/http-*`, `tests/node/net-*`, `tests/node/dgram*`, `tests/node/dns.test.ts`, `tests/node/https.test.ts`, `tests/node/tls.test.ts`, `tests/node/http2-constants.test.ts` | Heavy local coverage. Added `tls.setDefaultCACertificates`; listener-heavy tests may hit sandbox `EPERM`. |
| `unit_node/stream`, `string_decoder`, `zlib`, `vm`, `v8`, `worker_threads`, `sqlite` | `tests/node/stream-*`, `tests/node/string-decoder.test.ts`, `tests/node/zlib.test.ts`, `tests/node/vm.test.ts`, `tests/node/v8.test.ts`, `tests/node/worker-*.test.ts`, `tests/node/sqlite.test.ts` | Broad coverage. Added zlib empty-dictionary stream close and sqlite Buffer/file URL database paths. |
| `unit_node/module`, `events`, `async_hooks`, `domain`, `console`, `perf_hooks`, `child_process`, `buffer`, `path` | `tests/node/module.test.ts`, `events-edge.test.ts`, `async-hooks.test.ts`, `domain.test.ts`, `console.test.ts`, `perf-hooks.test.ts`, `child-process.test.ts`, `buffer-path.test.ts`, `path-edge.test.ts` | Broad local coverage already maps most useful upstream cases. |
| `integration` | `tests/cts`, `tests/deno`, `tests/npm`, CLI tests | Rust harness tests cannot be run directly; use as scenario catalog for cno CLI/cache/task/run/eval/install/npm coverage. |
| `specs` | `tests/cts`, `tests/npm`, `tests/deno`, `tests/jsr` | Large CLI fixture tree. Added coverage for `task` shell fallback/additional args/pre-post/dependency graphs, `test` filter/fail-fast/no-files flags, `run` local query/hash imports and BOM sources, `cache` JSON import scanning, `node` package imports slash wildcards, and npm/lockfile transferable resolver semantics. Needs continued scenario-by-scenario mining, especially remaining `npm`, remaining `run`, remaining `test`, remaining `task`, remaining `node`, and non-typechecker `check` cases. `check` is mostly Deno's TS checker; mine only resolver/source/runtime cases that apply to cno. |
| `napi` | `tests/npm/native-*`, native loader work | Upstream fixture requires native build harness. Use for N-API behavior gaps after local native fixture path is stable. |
| `ffi` | `tests/deno/ffi-pointer.test.ts`, `tests/npm/native-ffi-zeromq.test.ts` | Upstream FFI tests require compiled fixture library; partial semantic coverage only. |
| `sqlite_extension_test` | `tests/node/sqlite.test.ts`, native extension docs/tests | Extension loading is separate from core `node:sqlite`; upstream fixture requires compiled extension. |
| `node_compat` | `tests/node`, `tests/npm` | Mostly runner/config infrastructure; use summaries as compatibility checklist, not direct test source. |
| `wpt` | `tests/webapi` | Full WPT runner is out of scope; mine targeted Web API behavior only. |

## Explicitly Skipped Or Deferred

These upstream unit areas are currently unsuitable for direct local tests because the
runtime has no matching API, the test is Deno-internal, or it requires a heavy
external harness:

- Deno internals: `Deno[Deno.internal].core`, promise hooks, lint plugins, jupyter.
- Unsupported platform APIs: WebGPU, ImageBitmap/createImageBitmap, QUIC.
- Native fixture harnesses: full `napi`, `ffi`, `sqlite_extension_test`.
- Deno/Node internal binding fixtures: `specs/node/pipe_open_fd` depends on `internal/test/binding().internalBinding("pipe_wrap")` plus socketpair FFI and is not a normal public API surface for cno tests.
- Deno Rust CLI harnesses in `integration` and most `specs` expected-output tests.
- Deno test CLI features that cno does not implement as matching output surfaces yet: documentation tests, markdown tests, JUnit/TAP/dot output formats, captured-output formatting, permission prompts/sanitizers, and TS type-check-only behavior. This includes `specs/node/node_test_module_no_sanitizers`, whose useful `node:test` API behavior is covered but whose expected output is specifically Deno sanitizer reporting.
- Full `node:test` runner output parity from `specs/node/node_test_module` is deferred; cno now covers the useful module API surface, but not Deno's expected-output formatting for intentionally failing/cancelled tests.
- Runtime lifecycle/event gaps: `specs/run/_058_tasks_microtasks_close` is deferred because `globalThis.close()` currently exits immediately instead of Deno's softer event-loop close semantics; `specs/node/node_process_beforeexit_exit_events_emitted_without_listeners`, `specs/node/web_node_events_dispatched_in_correct_order`, `specs/node/unhandled_rejection_web`, `specs/node/unhandled_rejection_web_process`, and `specs/node/rejection_handled_web_process` are deferred until normal-exit `beforeExit`/`exit` and rejection events are bridged across Node `process` and Web event targets.
- Internal debug-output formatting: `specs/node/node_debug` mostly asserts `NODE_DEBUG=*` stream-internal diagnostic text. cno covers `node:util.debuglog` aliases but does not currently mirror Node/Deno stream debug transcript formatting.
- Type-check-only package metadata diagnostics: `specs/node/types_req_export` and `specs/node/types_versions` are TS checker/type-resolution tests for `types@...` conditions and `typesVersions`, not cno runtime behavior.
- Platform/permission-only cases: `specs/node/windows_write_stream_permissions` and `specs/node/global_cache_node_modules_perm_checks` assert Deno permission prompts/scoping around Node modules. cno currently treats permissions as granted and has separate `Deno.permissions` compatibility coverage.
- JSON-without-attribute diagnostics from `specs/run/import_attributes_static_error` and `specs/run/import_attributes_dynamic_error` are deferred because cno still permits existing `package.json`/JSON module imports without `with { type: "json" }`.
- Deno workspace installer/config behavior in `specs/npm/workspace_*`: cno currently resolves local `node_modules`/cache/package semantics but does not implement Deno's workspace membership and `workspace:` dependency graph.
- `Array.fromAsync` parity is deferred because touching the current native intrinsic triggers a QuickJS `invalid version (26 expected=27)` runtime failure; local async-iterator coverage uses direct `for await` until the engine intrinsic is repaired.
- `specs/run/import_blob_url_jsx` JSX output parity is deferred. `blob:` module loading is covered, but cno's current JSX transform/runtime output does not match Deno's classic `React.createElement` expectation in that fixture.
- Very large stress tests unless they cover a current bug class.

## Recent Additions

- `node:crypto`: finalized `Hash`/`Hmac` state semantics and EC explicit parameter rejection.
- `node:tls`: `setDefaultCACertificates()` validation and default CA propagation into `SecureContext`.
- `node:zlib`: `createDeflate({ dictionary: Buffer.alloc(0) })` close behavior.
- `node:sqlite`: `DatabaseSync` accepts Buffer/Uint8Array and `file:` URL paths.
- `specs/node/require_esm_module_exports`: CTS CJS `require(esm)` honors the ESM `"module.exports"` named export, including `undefined`, and transitive CJS re-export.
- `specs/task/additional_args*`, `specs/task/pre_post`, `specs/task/deno_no_pre_post`, `specs/task/dependencies/*`: CTS task runner falls back to the platform shell for Deno-compatible task commands, appends extra args literally, runs package.json `pre<task>`/`post<task>`, skips deno.json pre/post, dedupes diamond dependencies, and rejects dependency cycles.
- `specs/test/filter`, `specs/test/fail_fast`, `specs/test/no_files`: CLI test runner supports `--filter`, `--fail-fast`, `--permit-no-files`, fails empty test discovery by default, and keeps fail-fast serial across files.
- `specs/test/no_run`, `specs/test/non_error_thrown`, `specs/test/aggregate_error`: Deno test harness skips `noRun` entries at execution time, treats non-Error throws as failing tests, and preserves `AggregateError` identity/message in failure output.
- `specs/run/_075_import_local_query_hash`, `specs/run/byte_order_mark`, `specs/run/_088_dynamic_import_already_evaluating`, `specs/run/default_ts`, `specs/run/if_main`, `specs/run/import_meta`, `specs/run/exit_code`, `specs/run/exit_error42`: local file resolution strips query/hash from filesystem paths while preserving module identity, CLI run handles BOM-prefixed TypeScript sources, concurrent dynamic imports of an evaluating module share one evaluation, run supports stdin/default extensionless TypeScript plus `--ext=ts` entries, entry modules expose `import.meta.main`/file metadata/local resolution, and Deno exit codes propagate to the process.
- `specs/test/default_ts`: CLI test accepts direct extensionless TypeScript test files and `--ext=ts` test entries.
- `specs/eval/eval_basic`, `specs/eval/eval_p`, `specs/eval/eval_ts`, `specs/eval/cjs`: CLI eval runs inline code, prints expression results with `-p`, honors `--ext=ts`, and supports CJS/CTS eval source that can require local files.
- `specs/task/cwd`, `specs/task/init_cwd`, `specs/task/init_cwd_already_set`, `specs/task/exit_code_5`: CLI task now separates `--config` lookup from `--cwd` execution directory, preserves inherited/default `INIT_CWD`, and propagates shell fallback exit codes.
- `specs/cache/json_import`: static dependency scanning includes import attributes such as `with { type: "json" }`.
- `specs/node/imports_slash_wildcard`: package `imports` wildcard resolution covers `#/*` to `./src/*.js`.
- `specs/npm/require_resolve_url_paths`, `specs/npm/pkg_json_imports`, `specs/npm/reserved_word_exports`: `require.resolve(id, { paths })` treats string and `file:` URL paths as search directories, local package `imports` resolves `default` conditions from a package parent, and ESM import of CJS preserves reserved/special export names such as `"module.exports"`.
- `specs/npm/node_modules_dir_require_main_entry`, `specs/npm/local_dir_resolves_symlinks`, `specs/npm/bin_entries_prefer_closer`: local `node_modules` packages load package.json `main`, symlinked local package parents resolve private dependencies from the real package directory, and bin resolution prefers closer `node_modules/.bin` entries over persisted lock bins.
- `specs/lockfile/auto_discover_lockfile`, `specs/lockfile/no_lock`, `specs/lockfile/frozen_lockfile`: CTS runtime reuses an existing project `cts.lock` read-only for run-like flows, `disableLock` skips the project lock, and frozen resolution rejects modules missing from the lock.
- `specs/task/cwd`, `specs/task/init_cwd`, `specs/task/init_cwd_already_set`, `specs/task/exit_code_5`, `specs/task/non_existent`: CTS tasks run from the config root, default `INIT_CWD` to the invocation directory unless inherited or task env overrides it, propagate shell exit codes, and fail unknown task names.
- `specs/node/cjs_key_escaped_whitespace`, `specs/node/next_tick_uncaught_exception`: ESM import of CJS preserves escaped whitespace export names and `process.nextTick()` exceptions are delivered through `uncaughtException` listeners.
- `specs/run/import_data_url`, `specs/run/import_dynamic_data_url`, `specs/run/import_attributes_static_import`, `specs/run/import_attributes_dynamic_import`, `specs/check/bytes_and_text_imports`: Runtime coverage now exercises static and dynamic data URL TypeScript modules, JSON import attributes, and raw `text`/`bytes` module views over the same local specifier. QuickJS now forwards import attributes through module normalization so CTS can resolve separate source/text/bytes views; type-check diagnostic parity remains out of scope.
- `specs/run/mts_dmts_mjs`, `specs/run/cjs/main_module`, `specs/run/cjs/unprepared`: CLI run covers `.mts` entries importing `.mjs` plus `.d.mts` type-only modules, CJS entry `require.main` identity/id/filename, and non-statically-analyzable dynamic import of `.cjs`.
- `specs/run/env_file`, `specs/run/preload_modules`, `specs/run/preload_same_module`, `specs/run/top_level_await`, `specs/run/dynamic_import_pending_tla`, `specs/run/dynamic_import_error_cached`, `specs/run/dynamic_import_already_rejected`, `specs/run/stdin_read_all`, `specs/run/stdout_write_all`, `specs/run/stdout_write_sync_async`: CLI run now covers repeated `--env`/`--env-file` loading with interpolation/multiline/override/warning behavior, Deno-style `--preload` ordering before Node `--import`, same-module preload dedupe, pending TLA dynamic import namespace sharing, cached dynamic import failures/rejections, and concurrent stdio read/write behavior.
- `specs/run/_015_duplicate_parallel_import`, `specs/run/_016_double_await`, `specs/run/_018_async_catch`, `specs/run/_028_args`, `specs/run/dynamic_import_written_runtime`, `specs/run/dynamic_import_dep_written_runtime`, `specs/run/dynamic_import_dep_written_runtime_non_analyzable`: CLI run covers 100-way duplicate dynamic import identity/instantiation, multiple awaited filesystem ops, catching sync throws through `await`, post-entry `Deno.args` including `--`, and runtime-written dynamic import sources/dependencies across repeated runs.
- `specs/run/package_json_type/commonjs/basic`, `specs/run/cts/main`, `specs/run/cts/import_export_equals`, `specs/run/cts/cjs_import_cts`, `specs/run/fix_js_import_js`, `specs/run/fix_js_imports`, `specs/run/ts_import_from_js`, `specs/run/js_without_extension`: CLI run now covers package `"type": "commonjs"` `.js` files that switch to ESM when they contain static ESM syntax, `.mts`/`.cts` OXC/Sucrase classification, CTS `export =` and `import = require()` runtime behavior, CJS requiring `.cts`, JS re-export/import from TS modules in a Deno project, AMD-like JS imports without false dependencies, and extensionless `--ext=js` entries.
- `specs/run/main_module`, `specs/run/process_env_load`, `specs/run/node_options_require_flag`: CLI run covers `Deno.mainModule` staying pinned to the entry module from dependencies, `node:process.loadEnvFile()` / `process.loadEnvFile()` using the same env-file parser as `--env-file`, and `NODE_OPTIONS` `--require` / `-r` preloads executing before CLI `--require`.
- `specs/run/require_modules_multiple`, `specs/run/require_esm`, `specs/run/private_field_presence`, `specs/run/es_private_fields`, `specs/run/weakref`, `specs/run/import_common_js`, `specs/run/npm_pkg_requires_esm_js`: CLI run now covers repeated CJS `--require` preloads with `__filename`/`__dirname`, CJS `require()` of sync ESM and rejection of TLA ESM, private-field brand checks, JS private fields, `WeakRef`/`FinalizationRegistry`, ESM package entry importing local CJS, and package CJS requiring local `.js` sources through the ESM pipeline. Full Deno parity for bare `require` being undefined inside ESM is deferred because CTS currently exposes a global Node-compatible `require` for many local Node tests.
- `specs/run/wasm_module/cjs_importing`, `specs/run/wasm_module/imports`: CLI run now covers local `.wasm` imports from ESM, CJS `require("./mod.wasm")`, and CTS `import = require("./mod.wasm")` using the same wasm module loader. Remote wasm, lockfile integrity diagnostics, import-map wasm diagnostics, and type-check-only wasm diagnostics remain deferred.
- `specs/node/errors_have_code`: Local CJS `require.resolve()` respects package `exports` for node_modules package subpaths and reports `ERR_PACKAGE_PATH_NOT_EXPORTED`.
- `specs/node/dynamic_import_and_require_dual`, `specs/node/cjs_dynamic_import_esm_with_exports`, `specs/node/imports_wildcard`, `specs/check/npm_pkg_empty_main_entry`: Local node_modules packages now have coverage for `exports` import/require conditions, CJS dynamic import resolving an ESM package export, package `imports` wildcard resolution from CJS internals, and empty `main` fallback to `index.js`.
- `specs/node/detect_es_module_defined_as_cjs`, `specs/node/require_esm_reexport_esm`, `specs/node/require_esm_reexport_esm_module_exports`, `specs/node/require_export_from_parent_with_no_filename`: Local node module coverage now detects ESM syntax in no-`type` package `.js` files without breaking plain CJS package defaults, preserves names/defaults through CJS re-export chains of `require(esm)`, preserves ESM `"module.exports"` named export through a CJS re-export chain, and resolves package requires from `Module._compile()` with a relative fake filename.
- `specs/npm/imports_package_json`, `specs/npm/exports_no_condition_matched_error`, `specs/npm/error_version_after_subpath`: package `imports` misses expose `ERR_PACKAGE_IMPORT_NOT_DEFINED`, package `exports` root entries with no matching condition expose `ERR_PACKAGE_PATH_NOT_EXPORTED`, and `npm:pkg/subpath@version` is rejected with a Deno-compatible suggestion.
- `specs/npm/cjs_module_export_assignment`, `specs/npm/cjs_module_export_assignment_number`, `specs/npm/cjs_invalid_name_exports`, `specs/npm/cjs_this_in_exports`: ESM import of CJS preserves object/primitive `module.exports` namespace shape, non-identifier export names, `"module.exports"`, and bare named function `this` behavior.
- `specs/node/import_cjs_file_no_ext_mjs_pkg`: package subpath fallback can load an extensionless CJS file from a `type: "module"` package and expose its named exports to ESM.
- `specs/npm/pkg_index_ts_and_js`, `specs/npm/different_nested_dep_node_modules_dir_false`, `specs/npm/different_nested_dep_node_modules_dir_true`: npm package directory subpaths prefer `index.js` before TS/JSON/WASM siblings, and nested npm parents resolve dependency ranges from their own package metadata rather than the project root.
- `specs/npm/conditional_exports`, `specs/npm/conditional_exports_node_modules_dir`, `specs/npm/require_type_commonjs`, `specs/npm/cjs_reexport_collision`: local node_modules packages now cover conditional export root/subpath/pattern entries, ESM named imports from `type: "commonjs"` packages, and CJS `default` property collision preservation on default imports.
- `specs/node/readline_promises`, `specs/node/node_fs_cp`, `specs/node/child_process_node_cli_args`: node module coverage now includes `node:readline/promises` async iteration, repeated recursive `fs.promises.cp()` into the same destination, and `process.execPath` Node-style eval/print/no-warning/V8 flag handling including `-p`, `--print`, and `-pe`.
- `specs/node/child_process_fork_conditions`, `specs/node/stdio_ipc`, `specs/node/worker_threads_cache`: child-process coverage now propagates `fork({ execArgv })` custom conditions into child package export resolution, supports IPC stdio entries outside fd 3 including `['ipc', ...]` with ESM `node:process`, and worker coverage verifies independent dynamic import cache state per worker runtime.
- `specs/node/child_process_fork_deno_args`, `specs/node/readline_doesnt_require_permissions`, `specs/node/child_process_extra_pipes`: `fork()` accepts Deno-style `execArgv` such as `run -A --conditions=...` without double-translating the child process, `node:readline` callback interfaces are covered as async iterables over `Readable` input, and `spawn({ stdio })` exposes fd3+ extra pipe entries by their `child.stdio[index]` fd with bidirectional read/write coverage.
- `specs/node/http_agent_keepalive`, `specs/node/http_agent_keepalive_stale_retry`: `node:http` Agent now covers explicit keep-alive connection reuse, `req.reusedSocket`, `agent.freeSockets[name]` pool tracking, and stale idle socket recovery before the next request is exposed as a user error.
- `specs/node/node_test_mock`, `specs/node/node_test_module`: `node:test` is now a builtin polyfill backed by the cno test runner, with coverage for default/named helper identity, nested `t.test()`, `t.assert`, done-callback success, and `mock.fn()` / `mock.method()` call tracking, errors, `this`, `times`, `reset()`, `resetCalls()`, individual restore, and `restoreAll()`.
- `specs/node/missing_ext_suggestion`, `specs/node/esm_dir_import`, `specs/node/byonm_phantom_dep_res_failure`: CTS npm/local package resolution now keeps ESM package subpaths strict: it does not append `.js` / `.mjs` / `.cjs` or fall through to directory `index.js` for ESM imports, while preserving exact extensionless files and CJS `require()` fallback behavior.
- `specs/node/cjs_reexport_node_condition`, `specs/node/cjs_reexport_same_specifier_in_sub_folder`: node module coverage now verifies `node` export conditions selecting an MJS wrapper that re-exports CJS named exports, CJS getter re-exports from subfolders through the ESM namespace bridge, and local package `exports` entries keeping package-relative imports anchored to the same local `node_modules` package instead of falling back to registry metadata.
- `specs/node/wrapped_http_response`, `specs/node/worker_threads/message_port_removelistener`, `specs/node/cjs_analysis_multiple_errors`, `specs/node/spawn_keeps_npm_state`: local tests now cover a Hono-style wrapped global `Response` being written through `node:http` `ServerResponse`, `MessagePort.off("message")` removing a listener without hanging the port, deterministic reporting of the first missing dependency in a CJS re-export analysis chain, and a package entry spawning a child cno process that still resolves dependencies relative to the originating local package.
- `specs/node/unstable_detect_cjs`: local CLI/module coverage already exercises cts `import = require()` runtime behavior, `.cts` / `.mts` classification, package CJS requiring JS/ESM sources, and top-level return behavior through CJS execution. Deno's `--unstable-detect-cjs` and `DENO_COMPAT` flag/type-check switches are not modeled as separate runtime modes in cno.
- `specs/npm/deno_run_bin_cjs`, `specs/npm/deno_run_bin_esm`, `specs/npm/deno_run_bin_no_ext`, `specs/npm/deno_run_bin_special_chars`, `specs/npm/deno_run_bin_cjs_no_bin_entrypoint`, `specs/npm/deno_run_bin_esm_no_bin_entrypoint`, `specs/npm/deno_run_no_bin_entrypoint`, `specs/npm/deno_run_no_bin_entrypoint_non_existent_subpath`, `specs/npm/deno_run_non_existent`, `specs/npm/run_existing_npm_package`: CLI run now resolves `npm:` package bin keys from cached packages, including scoped packages, explicit versions, single-bin defaults, special-character bin names, extensionless CJS bin files, direct package file subpaths, and root packages with no bin entrypoint failing cleanly. `cno exec --cache-dir` now feeds the same cache root into `BinResolver`.
- `specs/npm/dynamic_import_deno_ts_from_npm`, `specs/npm/adding_npm_dep_in_dynamic_import`, `specs/npm/dynamic_import_json`: CLI run now covers an npm package function dynamically importing local `file:` TS/MTS URLs, a non-analyzable local dynamic import introducing an additional npm dependency at runtime from the same cache root, and JSON import attributes over `npm:pkg/package.json`.
- `specs/npm/unanalyzable_dynamic_import`, `specs/npm/dynamic_import_reload_same_package`: CLI run now covers variable-based direct dynamic `import("npm:...")` resolving from the configured cache and a static npm import plus runtime dynamic import of another local module that imports the same package, preserving one cached package instance even under `--reload`.
- `specs/npm/specifier_two_slashes`, `specs/npm/require_json`, `specs/npm/cjs_pkg_imports`, `specs/npm/builtin_module_module`, `specs/npm/cjs_with_deps`, `specs/npm/cjs_sub_path`, `specs/npm/cjs_require_esm`, `specs/npm/cjs_require_esm_mjs`, `specs/npm/create_require`: local cached npm package coverage now exercises unversioned scoped npm resolution through cached metadata, CJS `require("./data.json")`, package `imports` from CJS internals, bare `module`/`node:module` builtin access after `setup` has populated the selected cache, CJS package dependency/subpath resolution including `require("..")`, CJS requiring sync ESM, and package-internal `createRequire()` input validation plus local file/package.json loading.
- `specs/npm/cjs_import_dual`, `specs/npm/esm_import_cjs_default`, `specs/npm/dynamic_npm_resolution_failure`, `specs/npm/exec_file_inside_npm_package`, `specs/npm/compare_globals`, `specs/npm/remote_npm_specifier`, `specs/npm/mixed_case_package_name`, `specs/npm/bytes_and_text_imports`: CLI run now covers npm `import`/`require` conditional exports from a CJS package parent, ESM default/named imports from CJS packages plus ESM packages importing those CJS defaults, catchable dynamic npm resolution failures originating inside npm package code, npm package code spawning the current cno executable via `node:child_process`, package-visible `globalThis`/`process` basics, relative local modules importing npm packages, mixed-case scoped package resolution through cached metadata and dependencies, and package subpath `bytes`/`text` import attributes.
- `specs/npm/run_existing_npm_package_with_subpath`, `specs/npm/sub_paths`, `specs/npm/npm_pkg_depend_dep_same_name`, `specs/npm/node_modules_dir_require_added_node_modules_folder`: CLI run now covers unversioned `npm:pkg/bin-subpath` execution using cached package metadata and package.json dependencies, npm package export subpaths such as `pkg/server`, project package names not shadowing same-named npm dependencies, and package CJS `createRequire()` seeing project `node_modules` folders created at runtime.
- `specs/npm/lossy_utf8_module`, `specs/npm/lossy_utf8_script`, `specs/npm/lossy_utf8_script_from_cjs`: npm ESM and CJS package source files with invalid UTF-8 bytes decode with replacement characters and continue evaluating instead of aborting module loading.
- `specs/npm/cached_only`: `cno run --cached-only` now propagates to CTS npm resolution and rejects packages whose metadata or package directory is not already cached, without attempting a network download.
- `specs/npm/invalid_package_name`, `specs/npm/dynamic_import_invalid_package_name`, `specs/npm/non_existent_dep_version`: CLI run now covers invalid static npm specifiers failing before entry completion, invalid dynamic npm specifiers being catchable with normal `try/catch`, and missing exact npm versions/ranges failing instead of falling back to `latest`.
- `specs/npm/nonexistent_file`, `specs/npm/nonexistent_file_node_modules_dir`: CLI run now covers missing npm package subpaths failing without falling back to another file, both for cached npm packages and packages discovered from local `node_modules`.
- `specs/npm/node_modules_import`, `specs/npm/node_modules_import_auto`, `specs/npm/byonm_npm_specifier_in_node_modules`: local `node_modules` packages now keep one module identity across bare package imports, direct `./node_modules/...` file imports, and package subpath imports; `npm:pkg@range` can also satisfy semver ranges from an existing local `node_modules` package without falling back to the registry.
- `specs/npm/worker_shutdown_during_npm_import`: Web Worker runs now inherit the parent runtime's resolver/cache config, so npm imports inside workers can use the same local cache as the entry process. Worker `self.close()` is backed by the native worker runtime stop path, suppresses close-after-shutdown errors/rejections, and preserves already-posted messages while preventing user code after close from running.
- Web fetch/XHR protocol parity: `fetch()` and `XMLHttpRequest` now share a local protocol loader for `data:` and `blob:` URLs, including percent/base64 `data:` bodies and content-type propagation. This also protects data URL consumers such as worker/jsr/npm fixtures from curl-only protocol failures.
- `specs/run/import_blob_url`, `specs/run/import_blob_url_import_relative`, `specs/worker/worker_doest_stall_event_loop`, `specs/worker/worker_message_handler_error`, `specs/worker/worker_terminate_tla_crash`: CTS now resolves `blob:` object URLs as module sources from the shared Web object-URL store, Worker construction snapshots parent-thread blob URLs into data URLs, worker-created blob modules can be dynamically imported without stalling the event loop, message-handler errors dispatch cancelable `Worker#error` events, and immediate termination of pending-TLA workers is stable.

## Next Sweep Targets

- Remaining `specs/npm` registry/install/npmrc/workspace/link/dynamic-import/cache scenarios, remaining `specs/run`, remaining `specs/test`, remaining `specs/task` workspace/filter/bin/npx cases, remaining `specs/node`, and applicable non-typechecker parts of `specs/check` such as module-not-found/package resolver cases. CSS import diagnostics are deferred because cno currently treats unknown extensions as binary resource imports.
- `integration/*_tests.rs` scenario mining for CLI/cache/npm/task/run/eval parity.
- Native fixture strategy for N-API and FFI once local build/run wiring is stable.
